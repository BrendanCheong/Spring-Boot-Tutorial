spring.datasource.url=jdbc:postgresql://localhost:5432/student
spring.datasource.username=brendan
spring.datasource.password=amaterasu1234$
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
# To enable a connection to a postgresql database, first create a database in psql with CREATE DATABASE db_name
# Create whatever you want in this database using psql or Later using springboot, CREATE DATABASE yourdbname;
# in psql, GRANT ALL PRIVILEGES ON DATABASE yourdbname TO youruser; by default its "postgres"
# IF you want a custom username like "Brendan", remember that sql puts everything to lowerCase
# do \c db_name username_inLowercase to connect to db